# 猿人学 match 题号速查

> 面向"按题号刷题"场景：输入题号即可定位技术特征与已有案例。只收录已实测的题号；未做过的题走正常流程，解完后再补案例与经验沉淀。
> 平台更新后个别题可能有变，解题前仍按 CASE_LOOKUP/取证流程核实最新结构。

| 题号 | 题名/特征 | 技术要点 | 案例文件 | 还原方案 | 最后验证 |
|---|---|---|---|---|---|
| 4 | 雪碧图、样式干扰 | 无签名参数，仅 sessionid + 末页 UA=yuanrenxue；j_key 干扰过滤 + `index+left/8.5` 排序 + 像素哈希字典识别 | `cases/yuanrenxue-match4-sprite-pixelsort.md` | A 纯算还原 + DOM ground truth | 2026-08-23 |
| 5 | 修改版 MD5 + WAF Cookie | 魔改 MD5（T 常量注入 + `XMLHttpRequest.DONE*4` 分组步长，缺 XHR 时步长退化 1 是 token failed 根因）→ vm 沙箱补环境跑 decoded5.js 纯协议还原；蜜月期开窗同第 6 题 | `cases/modified-md5-xhr-done-yuanrenxue.md` | B vm 沙箱补环境（纯协议） | 2026-08-24 |
| 6 | AAEncode 混淆 + RSA 签名 + 蜜月期风控 | AAEncode 段仅产出 `window.o=1`（幌子）；m=二次 URL 编码的 RSA-1024("1\|"+t)，q 单段链 `1-<t>\|` 即可（多段链反被拒）；jsbn `am` 初始化被 try-catch 吞错需手动挂回；蜜月期开窗（GET HTML 约 5 秒、成功续期）+ 末页 UA=yuanrenxue | `cases/yuanrenxue-match6-aarcsa-honeymoon-risk.md` | B vm 沙箱补环境 + curl_cffi（纯协议） | 2026-08-24 |
| 7 | 动态字体（随风漂移） | 接口响应内嵌 base64 TTF + data 为 PUA 码点实体；码点每请求轮换但 `glyf.flags` md5 指纹跨字体固定 → 指纹字典法还原；请求侧全明文无签名，requests 直连（无 TLS 检测、无蜜月期、**勿开窗**：开窗 HTML 会 Set-Cookie 重置登录态致提交 401）；`m` 参数是 `/api/match/7`（404）hook 遗留可忽略；末页 UA=yuanrenxue | `cases/yuanrenxue-match7-dynamic-font.md` | A 纯算还原（内容还原型，Step 2 豁免） | 2026-08-24 |
| 9 | 动态 Cookie m（RSA+循环前缀） | 挑战 JS 含 `decrypt(ts)` 循环 N 次（N=2~5 随机）→ `m = prefix + encodeURIComponent(RSA_PKCS1_v1.5(ts)) + 'r'` 走 cookie；udc.js **定期更新**（公钥随之变，必须二进制抓取最新版，文本解码会损坏文件）；signer **禁止缓存 RSA**（缓存版 8/8 失败，无缓存模拟浏览器循环加密稳定通过）；数据**绑定 sessionid**（固定 session → 答案 27848571 不变）；sessionid 由 API 响应 set-cookie 下发；提交有随机拒绝需重试（≤8 次）；末页 UA=yuanrenxue | `cases/yuanrenxue-match9-dynamic-cookie2.md` | B vm 沙箱补环境（纯协议，无缓存循环加密） | 2026-08-25 |
| 10 | 瑞数 v3 变种 boot+api2 配套解密 + 重放攻击对抗 | rs.js(98KB ÿ 混淆) + api2/10($_ts 318KB 密文) + HTML 第 9 个 script 的 boot 引导器(73KB，**每次渲染不同，与 api2 同次渲染配套**，跨渲染混用→解密错乱→m 动态段全零) + meta challenge 拼装 m 生成链；m(281 字符，前 76 静态段)由瑞数 hook XHR.open 自动注入，**m 出现 qqq 零值填充段 = 补环境不完整信号**（曾因此误判「TLS 封死/会话强绑定」，Node 原生 https 直连全通已推翻）；每页响应 k.k="变量\|数值"须回写 window 参与下一页 m 生成；数据**绑定 sessionid**（答案 29178743）；末页 UA=yuanrenxue（软提示≠校验通过）；提交接口 `POST /a/10` **表单编码**；`<a>` href 须接真实 URL 解析、meta content 须回填真实值 | `cases/yuanrenxue-match10-ruishu3-replay-defense.md` | B vm 沙箱补环境 + D 环境伪装（纯协议，Node 原生 https） | 2026-08-26 |
| 12 | 入门级 js（页面内联明文签名） | `m = base64("yuanrenxue"+page)` 直接内联在 document.html:738（`m:btoa("yuanrenxue" + p)`），无 SDK 无混淆；**先读 document.html 内联 script 再谈 JS 文件级分析**；数据绑定 sessionid（同 session 数据恒定可复验）；末页 UA=yuanrenxue；提交 `POST /a/12` 表单编码；本题同时是 trace 信号形态/多进程合并/验证记录结构三个工具坑的实测来源（均已固化修复） | `cases/yuanrenxue-match12-inline-btoa.md` | A 纯算还原（requests 直连） | 2026-08-26 |
| 13 | 入门级cookie（eval 下发混淆 cookie） | `GET /api2/13`（同步 XHR，`document.html:744` `success:eval(data)`）每次下发一段 `document.cookie=('y')+(([] + ![])[1])+...` 字符串加法混淆脚本，解出 `yuanrenxue_cookie=<ts>|<随机串>`；**挑战脚本静态 JS 里不存在，靠 RuyiTrace eval 落盘 `logs/eval/eval_<pid>_<seq>_eval-direct.js` 拿全文+entry 行号**；混淆原子仅四类可穷举（字面量/`([] + ![])[i]`/`([] + !![])[i]`/`({} + "")[i]`），写 30 行求值器即可不碰 eval；令牌一次性禁缓存（缺失→`400 token failed`）；`m:window.match13` 是 hook 遗留、真实请求无 m；数据绑定 sessionid（答案 27121856）；末页 UA=yuanrenxue（普通 UA 返回中文提示数组）；提交 `POST /a/13` 表单编码 | `cases/yuanrenxue-match13-eval-cookie.md` | A 纯算还原（requests 直连） | 2026-08-26 |
| 14 | 动态 Cookie m/mz（指纹数组+哈希签名） | 静态混淆 m.js（553KB，jsencrypt 公钥内嵌）+ `GET /api2/14` 每请求前下发动态 JS eval 定义 `v14/v142`；`mz=btoa(53 字段指纹数组)`（navigator 25+screen 9+location 19，顺序与 m.js 一致），`m=md5hex\|b\|a\|n`（b=毫秒时间戳、a=b*8、n=window.n 计数器随 sp() 递增，**须持久沙箱**——每页重建沙箱 n 恒 1 → page2+ 400，page1 碰巧通过极具迷惑性，反模式 24）；**服务端只校验 mz↔m 自洽**（解码 mz 重算 md5hex 比对），指纹与真实浏览器 4 处差异照样接受（规则 25）；签名含 RSA 随机 padding 即走进错误分支，转 DIAGNOSE 回溯分支条件（反模式 23）；page5 UA=yuanrenxue 且**指纹 UA 必须同步改**（否则不自洽 400）；Firefox/ruyipage 全 API 400（UA 检测）→ MCP 真实 Chrome 兜底取证；数据绑定 sessionid（答案 31256141） | `cases/yuanrenxue-match14-fingerprint-cookie-m-mz.md` | B vm 沙箱补环境（持久沙箱，Node 原生 fetch） | 2026-08-28 |
| 15 | WASM 确定性签名 | 页面内联 JS 直接 `fetch('/static/new_match/question/15/main.wasm')` 加载 wasm（无混淆/JSVMP/环境检测，算法完整暴露在 document.html:712-769）；`m=encode(t1,t2)+'\|'+t1+'\|'+t2`（encode 为 wasm 导出 `(i32,i32)->i32`，**无导入纯确定性**，Node 原生 `WebAssembly.instantiate` 直跑、实例可复用，无需补环境）；`t1=floor(now/1000/2)`、`t2=t1-random(1..50)`；服务端重算 encode 比对 + t1 时效校验；**wasm 内联 base64 必须程序注入并 md5 核对**（手贴易损坏→token failed，且易误归因时间/会话，反模式 25），或独立文件加载；**Node 全局 fetch 禁混用 https.Agent**（undici 不兼容）；数据绑定 sessionid（答案 26550965）；末页 UA=yuanrenxue（普通 UA 返回中文提示数组非 400）；提交 `POST /a/15` 表单编码 | `cases/yuanrenxue-match15-wasm-deterministic-signature.md` | C WASM 加载（Node 原生 WebAssembly） | 2026-08-28 |
| 16 | webpack 混淆包签名（模块黑盒执行） | webpack 单行 bundle（310882B，sha256 `61db4701...`）+ obfuscator.io 混淆，签名只依赖模块 127（混淆 MD5→`window.md5`）与模块 732（126 字符变形 base64→`window.btoa`）；`m=d(15)+md5(btoa(t))+d(10)` **定长 57，前后缀随机不校验、中间 32 位 MD5 才是签名本体**，用途是 URL 查询参数而**非 Cookie**（Cookie 侧无签名）；`t=Date.parse(new Date).toString()` **秒级**（末三位恒 0，相邻秒会碰撞出相同 MD5 段但服务端同算法重算故不影响）；**三个必须踩准的坑**：① 模块 732 `case 1` 靠 `n.g` 选 try/catch 分支，`n` 是 webpack 的 `__webpack_require__`、`n.g=globalThis`，抠代码裸跑时 `n` 缺失 → 静默走 catch，**格式全对但服务端拒**（反模式 26，须 `new Function` 传 require 桩）；② `charAt` 索引最大 259 > 字母表 126，越界返回空串是算法语义，改用数组下标会拿到 `undefined`；③ `charAt`/切片边界——模块体边界 = 下一模块起点 - 2，多取一个 `}` 就 SyntaxError 且在超长单行里极难定位，追加 0~5 个 `}` 逐个 `node --check` 可自动定界；接口已从老版 `/api/match/16` 改为 `/api/question/16`（JS 内仍保留老常量，由模块 58 的 `$.ajax` hook 改写，**接口路径以本次抓包为准**）；page5 UA=yuanrenxue 且**必须校验 data 元素类型**（普通 UA 返回 HTTP 200 + 9 条中文提示字符串）；数据绑定 sessionid（答案 25052175） | `cases/yuanrenxue-match16-webpack-blackbox-branch.md` | B 最小 JS 沙箱（模块切片黑盒执行，Node 原生 https） | 2026-08-28 |
| 17 | 天杀的 Http2.0（传输层约束，请求侧全明文） | **请求侧零签名**：`GET /api/question/17?page=N&pageSize=10&kw=` 全明文，无 token/时间戳/随机量/指纹；**`m:window.match17` 是诱饵参数**——`$.ajax` hook 只在有人请求 `/api/match/17` 时才给 `window.match17` 赋值，该接口从未被请求（25 个包核对），值恒 `undefined` 被 jQuery `$.param` 丢弃，trace writer 参数全文可佐证，误当签名去逆就是无解方向（反模式 27）；真正约束在**传输层**：HTTP/2（题名明示，取证响应头 `x-firefox-spdy: h2`）+ 末页 UA=yuanrenxue + Cookie 带 sessionid；无 JS 写入的挑战 cookie（仅百度统计 `Hm_*`）；页面零混淆零 SDK，`crypto` trace 仅 2 条且来自第三方 SDK；实现用 Node 原生 `node:http2` 单 Session 复用 5 页后 `close()`、**不发 `accept-encoding`** 避免 br/zstd，`alpnProtocol==='h2'` 自检；**`--targets` 不能用 `question/17`**（静态资源 `static/new_match/question/17/webpack.js` 会误命中提前收尾），用 `page=1`/`page=2`；trace 里 XHR 记录被 transcend/mozilla 第三方脚本淹没，直接检索 URL 字面量定位 writer（`jquery.js:2391:23`）；自动 `--import-after` 只收进 823B 小文件，三个真正的 tab 进程日志在 kill 后 10~33 秒才刷盘，须核对 domtrace 各文件体积手动合并；数据绑定 sessionid（答案 25585231）；提交 `POST /a/17` 表单编码（`code=2` 通关，`1` 已做过） | `cases/yuanrenxue-match17-http2-transport-plaintext.md` | A 纯算还原（无可还原算法）+ E 传输层对齐（Node 原生 http2） | 2026-08-28 |
| 18 | jsvmp - 洞察先机（内联 JSVMP + 鼠标事件门控 + 末页双重校验） | 真实接口 `GET /api/v/question/18data?page=N`（页面上 `/api/match/18`、`/api/question/18`、`window.request` 全是连环诱饵，反模式 27 第四次实证）；内联 script #12 = `jsvmpzl v1.1.3` 自研 VM（34.5KB 单行，字面量全编码静态 grep 不可见），包装 `XMLHttpRequest.open` 追加 `&t=<服务器秒>&v=<非确定性 base64>`（Date.now 重写同步 `/api/getTime` 取 t；Math.pow 随机填充，v 无法重放比对只能服务端验收）；**VM 等真实鼠标**（mousemove/mousedown/mouseup 监听 + readyState=complete，不派发事件静默不签名）；**VM 字节码对 page 字面值 1/5 不签名**——末页 UA=yuanrenxue 触发 token 强校验，明文+改UA → 400 token failed，正确姿势 `page=05`（数值解析为 5，VM 照常签）+ 末页 UA；沙箱三大语义坑：window.BigInt 等内建须为自有属性 / navigator.webdriver 须挂原型（hasOwnProperty 须 false）/ 需派发鼠标事件（反模式 28，静默退出零报错，定位法见规则 28）；取证 JS 副本放 `result/src/target/original/`（质量门禁豁免路径）+ sha256 校验；数据绑定 sessionid（答案 32036118）；提交 `POST /a/18` 表单编码（code=2 通关） | `cases/yuanrenxue-match18-jsvmp-mouse-gated-signature.md` | B vm 沙箱黑盒执行 + D 环境伪装（Node 原生 vm + https） | 2026-08-28 |
| 19 | 乌拉乌拉乌拉（全明文 + Node TLS 指纹黑名单 + 末页 UA 分流） | 真实接口 `GET /api/question/19?page=N&pageSize=10&kw=`，响应明文 `{"data":[10 个整数]}`，页面零混淆零 SDK（`m:window.match19` 是诱饵——**`$.ajax` 内部 `k.extend(!0,{},{ajaxSettings},t)` 深拷贝的 `copy!==undefined` 守卫丢弃 undefined 键，真实请求无 m**，反模式 27 第五次实证且修正机理：`$.param` 对 undefined 渲染空串 `m=`（debug 文本可见），丢弃发生在深拷贝层，match17 的"`$.param` 丢弃"归因已修正）；**主坑在传输层：Node https/http2 客户端无论头部/时序/UA 如何复刻全部 400 `{"error":"token failed"}`（服务端 TLS ClientHello 黑名单）**——"token failed" 文案 ≠ 令牌参数缺失（match9 的 m-cookie 才是该文案的真实出处），跨客户端栈对照（curl/requests 200 vs Node 400）一轮定位，交付语言切 Python `requests.Session`（session.get/close 同样满足 Session 三件套门禁）；pages 1-4 任意正常 UA，page 5 必须 UA=yuanrenxue（普通 UA 返回 200 提示数组 `["请","将","UA",...]`，按 data 元素类型判别而非状态码）；数据绑定 sessionid（答案 27219355）；提交 `POST /a/19` 表单编码（code=2 通关） | `cases/yuanrenxue-match19-tls-fingerprint-blocklist.md` | A 纯算还原 + E 传输层对齐（Python requests） | 2026-08-29 |
| 20 | 2022 新年挑战（WASM wasm-bindgen 签名 + 服务器时间） | `GET /api/question/20?sign=...&t=...&page=...`，`sign=wasmSign(page+'|'+t)`（t=同步 `GET /api/getTime` 服务器毫秒文本）；wasm 从 `/api2/20` 运行时二进制拉取（取证通道 streaming 无字节 + 旧抓包产物文本化损坏，反模式 25 第二形态）；glue 原样还原 + Node 沙箱桩语义陷阱——wasm-bindgen get-global 链 `instanceof_Window` 须返回 true、document/body 须非空对象，否则 wasm trap `unreachable`（反模式 28 形态四，import 桩调用日志定位）；sign 32 位 hex 但**非** md5(page|t)（实测排除）；题面「试炼步骤」区块为唯一口径来源：全部 5 页求和 + 末页 UA=yuanrenxue；数据绑定 sessionid（答案 27713926）；提交 `POST /a/20` 表单编码（code=2 通关） | `cases/yuanrenxue-match20-wasm-bindgen-sign.md` | C WASM 加载（Node 原生 WebAssembly）+ B 最小沙箱（glue 还原） | 2026-08-29 |
| 21 | 守心 简单的指令（SM3 变体 + JSVMP 环境分支诱饵 + Firefox 内核毒化） | match3.js（match2023 第三题移植，525KB JSVMP）暴露 `window.SM3/sm3Digest/call`；`token = sm3Digest(str(服务器毫秒)+str(page))`，请求头 `accept-time` 传同值（**token 时间与 Accept-Time 必须同源**，浏览器里双轨时间源错位致浏览器自身 400）；SM3 变体常量（Chrome 真机分支）：IV[1]=7634d2c9/IV[4]=a10c30bc、T=[79dd4519,7c179d8a]、SS1 三项和 &0xFCFFFFFF 再 rotl 7（**Tj 先 rotl j 位**）、tt1&0xFFFFFFFA、tt2&0xFFAFFFFF、**strToBytes 字节偶数化**(c-c%2)；**Firefox 内核走诱饵分支**（IV[1]=IV[4]=7370067d 重复 + strToBytes 标准 ASCII，token 格式全对但服务端拒）——match3.js 用 Function.toString 检测 Object/Document/Window/Location/FocusEvent/Node/HTMLDocument/print 及 DOM 方法，桩须 nativize + 补原型链（反模式 29）；分支指纹 = `new SM3().reg` + `strToBytes('abc')` 与真机对比；Node https 直连本题未被 TLS 拦（与 19 相反）；数据绑定 sessionid（答案 29083835）；提交 `POST /a/21` 表单编码（code=2 通关） | `cases/yuanrenxue-match21-sm3-variant-decoy-branch.md` | A 纯算还原（SM3 变体常量真机提取）+ B 最小沙箱（过程对拍） | 2026-08-29 |
| 22 | 魔改标准算法（OpenSSL Salted + 字母表环境分支 + TLS 白名单） | `GET /api/question/22?page=N&pageSize=10&kw=&token=<44字符>&now=<13位ms>`；token=b64("Salted__"‖salt‖AES-256-CBC(EvpKDF-MD5("666yuanrenxue66",常量盐bb76994f²),now+page))，再过 22.js 内逆练MD5变体；**TLS 指纹白名单**（Node/requests 全 403 token failed，curl_cffi chrome131 过）；base64 字母表环境分支（混元表 vs 小写在前）；[Unforgeable] 探针；第 2 次计算反调试死循环（MCP 采样一次/会话） | `cases/yuanrenxue-match22-openssl-salted-alphabet-branch.md` | A 纯算还原 + B 最小沙箱 + 桥式交付（Python curl_cffi + Node 沙箱桥） | 2026-08-29 |

| 23 | js混淆源码乱码（toString 自引用解码 + 环境分派 IV/移位表/加法器的魔改 MD5） | `GET /api/question/23?page=N&pageSize=10&kw=&token=<32hex>&now=<13位ms>`，响应明文；**token=md5变体('/api/question/23'+now+page)**；obfuscator 短名混淆（无 _0x），解码器 `q=o+g` 读自身源码字节——**AST 反混淆产物禁执行**（重写即轮转死循环，反模式 31）；环境分派：window instanceof EventTarget/Window、document instanceof Document、**Firefox 不暴露 WindowProperties→_v fallback**、successAlert→标准移位表、createElement instanceof Node→掩码加法器；Node https 直连可用（无 TLS 白名单）；末页 UA=yuanrenxue | `cases/yuanrenxue-match23-selfref-decoder-env-dispatch.md` | B 最小沙箱（原码执行 + 一行导出桩 + 4 分支对齐） | 2026-08-29 |
| 24 | js混淆源码乱码（vmpzl 1.5.1 JSVMP 黑盒 + 状态数组常量偏移 XOR 30 就地修正） | `GET /api/question/24?page=N&pageSize=10&kw=&token=<part1>\|<part2>&now=<13位ms>`，响应明文；token=part1(base64)+`\|`+part2(去重值序列+字母间隔标记)；材料=`/api/question/24`+now+page；变换 out[i]=charCode[i]^K[i]（K 前缀链式密钥流，K[0..17] 固定常数）；**沙箱与浏览器唯一差异 = TL[11..] 恒差 XOR 30**（一次性状态偏移，逐位 diff 判分布定位，就地修正而非逐环节对齐——反模式 32 / 规则 30）；VM 探针走直达路径（分发器第一个形参的 `[1][0].time_list`）否则 OOM；Node https 直连被 TLS 指纹黑名单拒绝 → 桥式交付（Python requests + Node 沙箱桥）；**按页构建（跳过中间页，0.8~2.1s）才能落在服务端 2~4s 时效窗口内**（一次构建 5 页 7.6s 超窗 403）；末页 UA=yuanrenxue 且 token 生成环境 UA 同步 | `cases/yuanrenxue-match24-jsvmp-blackbox-tl-xor30.md` | B vm 沙箱黑盒执行 + E 桥式交付（Python requests） | 2026-08-31 |
| 25 | 控制流扁平化 VM 混淆 token（x 黑盒执行 + 环境桩必须在沙箱内运行） | `GET /api/question/25?page=N&pageSize=10&kw=&token=<base64\|编码串*编码串>&now=<13位ms>`；**token=x('/api/question/25'+now+page)**，x=25.js 顶层函数（48071B 控制流扁平化 VM 混淆，`for(;;) if(_$XX==...)` dispatcher），黑盒执行即可；token 结构 base64段\|Huffman编码段（char/frequency/left/right 字符+频次交替，含 g 等非 hex 字符）；x 内部 `(()=>_$AX=_$XL(+new Date))` 时间戳参与编码 → **生成时冻结 Date=getTime 返回的服务器时间戳**（服务端不校验时间窗口，T 偏移矩阵 now±100s 全 200，无需补偿）；**403 根因（反模式 34）：环境桩在主 realm 定义时 `win.window=globalThis` 指向主进程全局对象，x 的 `window.window==function(){return this}()` 自检失败误走 `_$VM=111` 分支 → token 全错**——环境桩必须在 `vm.runInContext` 沙箱内执行；环境对齐三要素：win.window=沙箱 globalThis / window.global 初始 undefined + 全局标识符 global 动态 getter / pgxList truthy 桩；**环境桩文件勿用 IIFE 包裹**（check_code_quality 把 IIFE 主体当单函数必超 90 行，拆 browser-objects/{dom,window,jquery,webapi}.js 顶层代码+具名函数）；`m:window.matchnumber` 是诱饵（/api/match/number 从未发出）；末页 UA=yuanrenxue；Node 原生 https 直连 | `cases/yuanrenxue-match25-cfa-vm-blackbox-env-realm.md` | B vm 沙箱黑盒执行（Node 原生 https） | 2026-08-31 |
| 26 | SM3 魔改 token（8 组环境分派 IV + 页面自驱动翻页） | `GET /api/question/26?page=N&pageSize=10&kw=&token=<64hex>&now=<13位ms>`；**token=SM3 魔改('/api/question/26'+now+page)**；与 match21 同族但环境分派扩到 **8 组**（reg[0..7] 分别检测 String(Document/Window/Navigation/Location/FocusEvent/require/Node/HTMLDocument) 的 native 串，typeof require 为 undefined 时 reg[5]=0x151137ad）+ _compress 掩码分支 `document.__proto__===HTMLDocument.prototype ? 0xfcffffff : 0xfceeeeee` + strToBytes 字节偶数化 `k & 0xfe`（page '2'/'3'→0x32、'4'/'5'→0x34 → 同 now 下 page2/3、page4/5 token **成对相同**，不是沙箱 bug）；**取证浏览器 token 必 403**（Firefox 内核走诱饵分支，勿无限重采，签名正确性交给 trace+沙箱+REAL_VERIFY 闭环）；页面自驱动翻页（jq 桩按 selector 缓存 + on() 记录 handler + 手动触发 click，复用页面页码计数器）；文件尾反调试 IIFE 需空 setInterval 桩；**环境桩拆独立文件**（禁大段模板字符串，check_code_quality 红线）；提交前先验活会话；末页 UA=yuanrenxue | `cases/yuanrenxue-match26-sm3-blackbox-page-drive.md` | B vm 沙箱黑盒执行（Node 原生 https） | 2026-08-31 |
| 28 | JSVMP 内嵌确定性 RSA-1024 token（字节码 limbs 直读纯算，无需跑 VM） | `GET /api/question/28?page=N&pageSize=10&kw=&token=<172B b64>&now=<13位ms>`；**token=RSA-1024 确定性密文**（128B），明文='/api/question/28'+now+'28$'+page，PKCS#1 填充 `00 02\|\|01×k\|\|00`（固定 0x01 无随机），**N 从字节码尾部 28-bit limbs 字面量直读重建**（出现序≠数组序需对拍修正），编码=JSBN hex2b64（3 hex→2 b64 非标准 base64）；对拍 capture 样本逐字节一致即确认；**数据绑定 sessionid**（换会话数据完全不同必须重算答案）；站点限流：短窗口连续请求约第 3 页起 403 token failed（单请求正常，非签名错），页间 3s+冷却+提交用 --answer 单请求解耦；提交 POST /a/28 表单编码 code=2 通关（答案 27673886） | `cases/yuanrenxue-match28-jsvmp-rsa-purecompute.md` | A 纯算（Node BigInt 模幂） | 2026-08-31 |
## 用法

- 刷题前：`node scripts/search_cases.js <题号或技术关键词>` 命中案例则重读踩坑记录 + 可验证事实清单，按最新结构核对后沿用。
- 首次做某题：正常走完整流程，完成后同步新增 `cases/` 案例 + 更新本表 + 追加 index.json 索引。
- 命中案例后仍须按 SKILL.md 状态机走完整流程，不得直接照搬算法（平台可能升级）。
- 第 4/5/6 题风控底座同源：蜜月期开窗（GET HTML 约 5 秒窗口、成功请求续期）、无页间隔要求（签名正确时无缝连发全过，"放慢速度"文案实为签名错误信号）、失败 5-10 次进惩罚期（冷却 10 分钟+）、末页 UA=yuanrenxue。**新题按此基线逐项验证，不是照搬步骤**——各题风控配置独立：match7 数据接口无蜜月期、无需开窗，且开窗 HTML 响应会 Set-Cookie 重置登录 sessionid 导致提交 401；match10 数据接口每页都要 m（与 match9 验证后免 m 不同），服务端页面加载时重置 sessionid（取数须带用户登录 sessionid，纯协议下固定 cookie 头贯穿全链即可，无需取证 profile）；迁移任何预热/开窗步骤前先直接请求一次数据接口验证是否必要（反模式 18）。
- **数据绑定 sessionid 是 match9/10 共性**：match10 复用取证 profile（sessionid 保留）后数据与匿名 session 不同，固定 session → 答案不变。match 题数据类接口先做 session 基线验证（反模式 19）。

## 相关参考

- `cases/_template.md`：新增案例骨架。
- `references/rendering/image-content-reversal.md`：图片型内容反爬通用方法（match/4 核心）。
- `references/rendering/font-anti-crawl.md`：字体反爬通用方法（match/7 核心：内嵌形态识别、字形指纹法、ground truth 验证阶梯）。
- `references/network/ip-risk-control.md`：会话状态类风控识别专节（蜜月期/惩罚计数/数据绑定 session 基线，match/5/6/9/10 实测）。
- `references/network/dynamic-resource.md`：黑盒加密 SDK 定期更新专节（公钥随版本轮换 + 二进制抓取纪律，match/9 核心，match10 rs.js/api2 同理）+ 会话配套资源专节（boot 与 api2 同次渲染配套，match10 实测）。
- `references/env/env-detect-bypass.md`：瑞数 v3 环境检测对齐探针法（match10 终局验证路径）。
- `references/env/env-object-model.md`：元素语义真实化（`<a>` href 真实 URL 解析 + meta content 真实值回填，match10 实测）。
- `references/hooks/anti-debug.md`：沙箱执行侧输出劫持（jsjiami 覆写 console.log，match/9 实测）。
- `references/workflow/experience-rules.md`：规则 21（DOM ground truth）+ 规则 10（签名哈希常量篡改降级信号）+ 规则 22（黑盒执行禁止缓存复用）+ 规则 24（黑盒执行禁止预填状态快照，match10 实测）+ 规则 25（指纹对齐验收线是"参数自洽"非"逐字节复刻"，match14 实测）+ 规则 26（webpack bundle 模块切片黑盒执行：切片定界 + 隔离作用域 + require 桩 + 反调试处理，match16 实测）+ 规则 27（请求侧无签名的三条判据 + 传输层题型：Node http2 要点 match17 实测、跨客户端栈对照法与交付语言切换 match19 实测）+ 规则 28（JSVMP 沙箱静默退出双层插桩定位法 + 语义级环境对齐清单，match18 实测）。
- `references/workflow/common-pitfalls.md`：反模式 16（补环境死循环诊断，含插桩 while(1) 禁令，match10 实测）+ 反模式 19（数据差异未先验 session 基线，match9/10 实测）+ 反模式 20（VM 卡死后转投浏览器黑盒取数，match10 为主体，含终局修正后记）+ 反模式 11（外部失败未验证签名内容就归因 TLS/会话强绑定，match10 为主体）+ 反模式 23（签名含服务端不可复算随机量 → 分支判定失败信号）+ 反模式 24（请求序号计数器随沙箱重建被重置，match14 双实证）+ 反模式 25（交付物内联黑盒资源完整性：wasm/base64 程序注入 + hash 核对，失败先 diff 两版本差异点，match15 实证）+ 反模式 26（抠代码后分支静默漂移：宿主/打包器注入对象缺失导致走错分支，格式全对却被拒，match16 实证）+ 反模式 27（诱饵参数：参数名存在 ≠ 参数生效，`m` 恒 undefined 被序列化/深拷贝层丢弃仍去逆算法，match7/13/17/19 四次实证且 match19 修正丢弃机理——`$.ajax` 深拷贝 `copy!==undefined` 守卫而非 `$.param`）+ 反模式 28（环境语义级偏差 → JSVMP 沙箱静默退出：内建非自有属性 / webdriver 误为自有属性 / 鼠标事件门控未派发，match18 实证）。
- **接口路径随站点版本变化**：老题解里的 `/api/match/N` 多已改为 `/api/question/N`（match16 实测：JS 内仍保留 `/api/match/16` 常量，由 `$.ajax` hook 改写）。外部老题解只作假设，**接口路径一律以本次抓包为准**。
- `cases/yuanrenxue-match22-openssl-salted-alphabet-branch.md`：**match22（魔改标准算法）**：OpenSSL Salted 格式（"Salted__"‖salt8‖ct16，44字符）+ EvpKDF-MD5 三块链（盐=常量 bb76994f²，fe() fallback）+ AES-256-CBC（明文=now+page）+ base64 字母表环境分支（混元表 vs 小写在前，同 Z.ct 双侧串 diff 定位，22.js 补丁暴露 __hd/__hs 桥内翻译）+ TLS 指纹白名单（未消费 token 三客户端阶梯）+ [Unforgeable] 沙箱对齐（delete window 探针）+ 第 2 次计算反调试死循环（MCP 采样一次/会话）。2026-08-29 通关（code=2，答案 26240105）。
